# 强制走代理：命中这里的域名/IP 强制从节点出去。
#
# 用途是反着来的例外 —— 某些域名被 ChinaDomain / GEOIP,CN / GEOSITE,CN 判成
# 国内地址走了直连，但实际必须翻墙才能访问，在这里把它们拉回代理。
# 这个规则集排在其它规则之前，所以优先级高于「全球直连」那几条。
#
# 由控制台（http://<服务器IP>:8787 的「代理例外」面板）读写，也可以手工编辑：
#   - 一行一条，格式：匹配方式,值[,no-resolve]
#   - 支持 DOMAIN / DOMAIN-SUFFIX / DOMAIN-KEYWORD / IP-CIDR / IP-CIDR6 / GEOIP
#   - 以 # 开头的是注释，空行会被忽略
#   - 手工改完需要让 mihomo 重新读取：控制台点「重新应用」，或
#     docker compose restart proxy
#
# 出口是控制台上的「🚀 节点选择」分组。如果那个分组被切成了 DIRECT，
# 这里写的域名照样会直连 —— 需要真出网时请确认该分组当前挂在节点上。

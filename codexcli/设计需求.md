接下来要开展codexcli的适配工作，类似的工作在vscode中开展过对codex的配置。
我的codexcli运行在linux环境下，windows上面没有运行。
我的需求就是codexcli能自动从gateway拉取模型列表，然后不用登陆，进入codexcli后，可以通过/model切换模型列表中的模型。
另外，也要考虑openai模型和custom-openai模型，custom-openai模型是私有化部署模型，其reasoning的强度仅支持low、high、max三级。另外，关于私有化部署接口有一些reasong_effort的配置，以及相关参数的配置，之前是有相关的开发和litellm的相关改动，具体你可以参考vscode下面的工程。
相关开发脚本和使用文档，要放在codexcli目录下。
